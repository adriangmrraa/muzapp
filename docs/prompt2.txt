Hacé un SDD init completo del proyecto muzapp. Seguí estos pasos:

1. Buscá en Engram: mem_search query="architecture/muzapp-resumen-ejecutivo" scope=project
   Si no existe, buscá: mem_search query="muzapp" scope=project

2. Leé TODAS las memorias de arquitectura que encuentres sobre muzapp.
   En particular: architecture/bot-ventas-system-prompt-v6, 
   architecture/sistema-promos-completo, architecture/telegram-bot-redesign,
   architecture/phone-normalization

3. Leé el stack del proyecto: 
   - package.json para ver dependencias
   - src/db/schema.ts para ver el modelo de datos completo
   - src/lib/whatsapp/agent.ts para ver el agente de WhatsApp
   - src/lib/telegram/ para ver el bot de Telegram
   - src/app/(admin)/admin/ para ver la estructura del admin
   - drizzle/*.sql para ver las migraciones existentes
   - sdd/ para ver todos los SDD existentes

4. Indagá la infraestructura:
   - .env para ver las variables de entorno
   - Render (hosting) + Neon (PostgreSQL) + Cloudinary (imágenes)
   - YCloud (WhatsApp API) + OpenAI (GPT + Whisper)
   - Telegram Bot API

5. Indagá la estructura del proyecto:
   - src/app/ (Next.js App Router)
   - src/components/ (componentes UI)
   - src/lib/ (lógica de negocio, agents, tools)
   - src/db/ (schema, seed, conexión)

6. Una vez que tengas contexto completo, decime exactamente qué entendiste
   y proponé los próximos pasos lógicos para avanzar en el proyecto.

Usá SDD para cualquier cambio que propongas: explore → proposal → specs → design → tasks → apply → verify.